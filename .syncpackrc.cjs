// @ts-check

/** @type {import("syncpack").RcFile} */
const config = {
  versionGroups: [
    {
      label: "Ignore everything",
      specifierTypes: ["range"],
    },
  ],
};

module.exports = config;
